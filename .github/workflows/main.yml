name: basicpipeline
on: 
 push:
   branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - name: checkout code
          uses: actions/checkout@v3
        - name: show files
          run: Is
        - name: step1
          run: echo "hello"
        
