name: basicpipeline
on: 
 push:
   branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - name: step1
          run: echo "hello"
        - name: step2
          run: echo "display pipeline"
